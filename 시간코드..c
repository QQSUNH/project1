#include<stdio.h>
#include<time.h>
int main()
{
	int time2 = time(NULL); //시작 시점
	printf("%d\n", time2);
	getchar();
	int time3 = time(NULL); //끝나는 시점
	printf("%d\n", time3);
	printf("%d\n", time3-time2); //소요시간 

}



if (TRYCOUNT == 5) {
	int hint = rand()%10
	while (hint == computer[0] || hint == computer[1] || hint == computer[2]) {
		hint = rand() % 10;

	}
	printf("힌트: %d는 정답이 아닙니다 \n", hint);
		


}